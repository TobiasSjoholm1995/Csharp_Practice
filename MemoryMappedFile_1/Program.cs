using MemoryMappedFile_1;
using System;

class Program {
    // Process A:
    // This is similar functionality as socket but faster
    // another design than socket but same functionality
    static void Main(string[] args) {
        new WriteChat();
    }
}